import SwiftUI

struct FontsList: View {
    @EnvironmentObject var appModel: AppModel
    
  
    
    var body: some View {
        
        VStack() {
        
        }
    }
    
   
}

#Preview {
    FontsList()
        .environmentObject(AppModel())
}
